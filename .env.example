# Bench testing secrets — copy to .env (which is gitignored) and fill in.
# Do NOT commit .env. The .env.example file IS committed as a template.

# WiFi password used by STA-mode test scripts (e.g. /mnt/c/tmp/daqifi_test/configure_sta.py)
WIFI_PASS=your_wifi_password_here
